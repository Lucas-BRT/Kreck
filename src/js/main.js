import {
    handleConnectButton,
    handleQrCodeButton,
    handleConfigButton,
    handleIpInput,
    handlePortInput,
} from "./handlers.js";

handleConnectButton();
handleQrCodeButton();
handleConfigButton();
handleIpInput();
handlePortInput();
